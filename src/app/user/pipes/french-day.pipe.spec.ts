import { FrenchDayPipe } from './french-day.pipe';

describe('FrenchDayPipe', () => {
  it('create an instance', () => {
    const pipe = new FrenchDayPipe();
    expect(pipe).toBeTruthy();
  });
});
